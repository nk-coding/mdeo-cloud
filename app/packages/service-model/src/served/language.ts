/**
 * Re-exports the model language plugin provider for the language server.
 * This file is served as an ESM module for the workbench language server to import.
 */
import { modelPluginProvider } from "@mdeo/language-model";

export default modelPluginProvider;
