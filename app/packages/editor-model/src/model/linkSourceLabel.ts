import { GLabel } from "@mdeo/editor-shared";

/**
 * Client-side model for link source endpoint labels.
 * Displays the label at the source end of a link edge.
 */
export class GLinkSourceLabel extends GLabel {}
