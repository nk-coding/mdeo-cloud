import { GLabel } from "@mdeo/editor-shared";

/**
 * Client-side model for property labels within an Object node.
 * Displays property assignments (name = value) in the diagram.
 */
export class GPropertyLabel extends GLabel {}
