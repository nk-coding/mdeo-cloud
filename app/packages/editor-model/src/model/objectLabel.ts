import { GLabel } from "@mdeo/editor-shared";

/**
 * Client-side model for the Object label.
 * Displays both the name and type of an object instance in the diagram.
 * Format: "name : type" (UML convention for instances)
 */
export class GObjectLabel extends GLabel {}
