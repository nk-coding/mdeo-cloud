import { GLabel } from "@mdeo/editor-shared";

/**
 * Client-side model for link target endpoint labels.
 * Displays the label at the target end of a link edge.
 */
export class GLinkTargetLabel extends GLabel {}
