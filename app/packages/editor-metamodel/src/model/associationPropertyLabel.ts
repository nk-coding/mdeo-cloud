import { GLabel } from "@mdeo/editor-shared";

/**
 * Client-side model for association property labels.
 * The label inside an AssociationProperty node.
 */
export class GAssociationPropertyLabel extends GLabel {}
