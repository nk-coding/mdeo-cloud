import { GLabel } from "@mdeo/editor-shared";

/**
 * Client-side model for association multiplicity labels.
 * The label inside an AssociationMultiplicity node.
 */
export class GAssociationMultiplicityLabel extends GLabel {}
