/**
 * Re-exports the model transformation language plugin provider for the language server.
 * This file is served as an ESM module for the workbench language server to import.
 */
import { modelTransformationPluginProvider } from "@mdeo/language-model-transformation";

export default modelTransformationPluginProvider;
