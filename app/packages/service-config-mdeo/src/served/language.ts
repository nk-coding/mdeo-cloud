/**
 * Re-exports the config MDEO language plugin provider for the language server.
 * This file is served as an ESM module for the workbench language server to import.
 */
import { configMdeoPluginProvider } from "@mdeo/language-config-mdeo";

export default configMdeoPluginProvider;
