/**
 * Inner padding (in pixels) between a match node's outline and its pattern content area.
 * Used by both the view renderer and the ELK autolayout engine to keep them in sync.
 */
export const MATCH_NODE_INNER_PADDING = 40;
