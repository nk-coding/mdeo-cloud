/**
 * Re-exports the script language plugin provider for the language server.
 * This file is served as an ESM module for the workbench language server to import.
 */
import { scriptPluginProvider } from "@mdeo/language-script";

export default scriptPluginProvider;
