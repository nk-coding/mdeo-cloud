/**
 * Re-exports the config language plugin provider for the language server.
 * This file is served as an ESM module for the workbench language server to import.
 */
import { configPluginProvider } from "@mdeo/language-config";

export default configPluginProvider;
