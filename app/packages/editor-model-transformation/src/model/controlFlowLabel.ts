import { GLabel } from "@mdeo/editor-shared";

/**
 * Client-side model for control flow labels.
 * Displays labels like "true", "false", "else" on control flow edges.
 */
export class GControlFlowLabel extends GLabel {}
