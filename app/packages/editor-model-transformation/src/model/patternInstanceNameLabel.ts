import { GLabel } from "@mdeo/editor-shared";

/**
 * Client-side model for pattern instance name labels.
 * Displays the instance name and optional type in format "name : type".
 */
export class GPatternInstanceNameLabel extends GLabel {}
