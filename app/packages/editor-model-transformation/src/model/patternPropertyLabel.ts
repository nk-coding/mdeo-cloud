import { GLabel } from "@mdeo/editor-shared";

/**
 * Client-side model for pattern property labels.
 * Displays property assignments within a pattern instance.
 */
export class GPatternPropertyLabel extends GLabel {}
