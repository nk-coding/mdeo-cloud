import { GLabel } from "@mdeo/editor-shared";

/**
 * Client-side model for pattern link end labels.
 * Displays the optional property specification at the link end.
 */
export class GPatternLinkEndLabel extends GLabel {}
