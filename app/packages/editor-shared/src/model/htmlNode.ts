import { GNode } from "./node.js";

/**
 * Client-side model for nodes that use HTML rendering.
 * Extends GNode to support HTML-based visual representation instead of pure SVG.
 */
export class GHtmlNode extends GNode {}
